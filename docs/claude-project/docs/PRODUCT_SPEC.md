# Product / Process Spec

> Source of truth for scope. Once this is filled in and work has started,
> treat it as **immutable** — don't edit it to match reality after the fact.
> If scope genuinely changes, record the change as an ADR in
> [DECISIONS.md](./DECISIONS.md) that supersedes the relevant section here,
> rather than silently rewriting this file.

<!--
TODO: paste or write the actual product brief. Suggested sections:

## 1. Problem / goal
What is this product for, who is it for, what does success look like.

## 2. Scope (v1)
What is explicitly in scope for the first shippable version.

## 3. Out of scope
What is explicitly deferred, and why (avoids relitigating it every session).

## 4. Core user flows
The primary paths a user takes through the product.

## 5. Non-functional requirements
Performance, security, accessibility, compliance constraints.

## 6. Process / phases (optional)
If the build is phased (e.g. Research → Architecture → Design System →
Foundation → Feature phases → Hardening), list them here with what each
phase must deliver before the next one starts. This is what CLAUDE.md's
"Process" section and PROGRESS.md's "Current phase" refer back to.

## 7. Working rules
Any project-specific rules beyond what's already in CLAUDE.md's working
rules (e.g. specific compliance requirements, specific vendor constraints).
-->
